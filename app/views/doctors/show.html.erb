<h2><%= @doctor.name %> <%= @doctor.department %> </h2>

<% @doctor.appointments.each do |appt| %>
  <%= appt.formatted_datetime %> | <%= link_to appt.patient.name, patient_path(appt.patient) %>
  <br />
<% end %>
